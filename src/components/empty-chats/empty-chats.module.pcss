.empty_chats {
    background-color: var(--background);
    display: flex;
    align-items: center;
    flex-direction: column;
    justify-content: center;
    gap: 24px;
}

.title {
    font-size: 20px;
    line-height: 24px;
    color: var(--light);
}